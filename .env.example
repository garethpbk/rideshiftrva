# MongoDB (Atlas)
DATABASE_URL="mongodb+srv://user:password@cluster.mongodb.net/rideshiftrva?retryWrites=true&w=majority"

# Auth.js
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET=""  # generate with: openssl rand -base64 32

# Email (nodemailer SMTP connection string)
EMAIL_SERVER="smtp://user:pass@smtp.example.com:587"
EMAIL_FROM="rideshiftrva@example.com"

# Vercel Cron (only needed in production)
CRON_SECRET=""  # generate with: openssl rand -hex 16
